# Fetch-API-Assignment
This is a basic web application that fetches user data from a public API and displays it on the page when a button is clicked.  Features  Uses JavaScript fetch() with async/await  Displays data dynamically  Load Data button  Basic error handling  API Used  https://jsonplaceholder.typicode.com/users
